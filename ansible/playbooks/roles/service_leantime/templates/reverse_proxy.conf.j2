server {
    include /etc/nginx/common/server_common.conf;
    server_name leantime.{{ external_hostname }};
    
    location / {
        set $upstream http://leantime:8080;
        proxy_pass $upstream;
        include /etc/nginx/common/proxy_common.conf;
    }
}
